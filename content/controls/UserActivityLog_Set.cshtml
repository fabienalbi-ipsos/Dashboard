@{
    if (Session["impersonation"] == null)
    {
        string ActivityType = Request["ActivityType"], ActivityName = Request["ActivityName"];
        db.UI_UserActivityLog_Set(ActivityType, ActivityName);
    }
}